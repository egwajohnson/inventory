

services:
  app:
    container_name: front
    build: .
    env_file:
      - .env
    ports:
      - "4000:4000"
    volumes:
      - ./src:/app/src
    depends_on:
      - mongo

  mongo:
    image: mongo
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"