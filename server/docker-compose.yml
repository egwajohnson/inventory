
services:
  app:
    container_name: Backend
    build: .
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - ./src:/app/src
    depends_on:
      - mongo

  mongo:
    image: mongo
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"